# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/

# Virtual envs
.venv/
venv/
env/
.env
.env.*
!.env.example

# uv (lockfile only when contributors use uv for the dev env; the
# project itself stays installable via pip per CI)
uv.lock

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Spotify-Wrapped-MCP runtime secrets (NEVER commit)
credentials.json
spotify-credentials.json
*.credentials.json
tokens.json
*.token
*.tokens
.spotify-cache

# History poller storage (user data)
history/
*.history.jsonl
data/
